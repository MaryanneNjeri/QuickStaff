import React from 'react';
import { Text, View } from 'native-base';

// eslint-disable-next-line react/prefer-stateless-function
export default class Error extends React.Component {
  render() {
    return (
      <View>
        <Text>Events Card</Text>
      </View>
    );
  }
}
